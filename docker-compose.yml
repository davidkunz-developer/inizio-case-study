version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - SERPAPI_API_KEY=${SERPAPI_API_KEY}
    # Automatický restart při změně kódu (pro vývoj)
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
